# ===========================================
# EXPERTLINE ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# ===========================================
# DATABASE
# ===========================================
# PostgreSQL database connection string
# Format: postgresql://username:password@host:port/database_name
# Example for local development:
DATABASE_URL="postgresql://username:password@localhost:5432/expertline_db"

# For production (e.g., Railway, Supabase, Neon, etc.):
# DATABASE_URL="postgresql://user:pass@host:5432/dbname"

# ===========================================
# NEXTAUTH CONFIGURATION
# ===========================================
# NextAuth secret key - generate a random string
# You can generate one at: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET="your-super-secret-key-here-min-32-chars"

# NextAuth URL - your app's base URL
# For local development:
NEXTAUTH_URL="http://localhost:3000"
# For production:
# NEXTAUTH_URL="https://yourdomain.com"

# Base URL for the application
# This is used for generating absolute URLs in the application
# For local development:
BASE_URL="http://localhost:3000"
# For production:
# BASE_URL="https://yourdomain.com"

# ===========================================
# OAUTH PROVIDERS
# ===========================================

# Google OAuth (optional but recommended)
# Get these from: https://console.developers.google.com/
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"


# ===========================================
# GOOGLE GEMINI API
# ===========================================
# Google Gemini API key for AI-powered code comparisons
# Get your API key from: https://makersuite.google.com/app/apikey
# This enables the AI Mode feature for intelligent code analysis
GOOGLE_GEMINI_API_KEY="your-google-gemini-api-key-here"

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================
# Node environment
NODE_ENV="development"

# ===========================================
# OPTIONAL: RATE LIMITING
# ===========================================
# The app uses in-memory rate limiting by default
# For production with multiple instances, consider Redis:
# REDIS_URL="redis://localhost:6379"

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================
# 1. Copy this file to .env: cp env.sample .env
# 2. Fill in all the required values above
# 3. Set up your PostgreSQL database
# 4. Run: npm install
# 5. Run: npx prisma generate
# 6. Run: npx prisma db push
# 7. Run: npm run dev
#
# For production deployment:
# - Set NODE_ENV="production"
# - Use secure, production database URLs
# - Set proper NEXTAUTH_URL to your domain
# - Set BASE_URL to your production domain
# - Ensure all secrets are properly secured
#
# BASE_URL Usage:
# - BASE_URL is used for generating absolute URLs in the application
# - If not set, it falls back to NEXTAUTH_URL
# - For local development: BASE_URL="http://localhost:3000"
# - For production: BASE_URL="https://yourdomain.com"
